<h1>Contributors</h1>
<p>Many people worked hard on this mod. Let's take a moment to appreciate their effort.</p>
<?php $currentHighestRole = null; ?>
<div class="row">
<?php foreach (${basename(__FILE__, '.phtml').'_contributors'} as $contributor) : ?>
	<?php
        if ($currentHighestRole !== $contributor->getRoles()[0]->name) :
            //New highest role
	        $currentHighestRole = $contributor->getRoles()[0]->name;
    ?>
    </div> <!-- ending for <div class="row"> -->
    <hr>
	<h2><?= $contributor->getRoles()[0]->name ?>s</h2>
    <div class="row">
	<?php endif ?>
	<div class="col-3"><a href="/cast/<?= $contributor->getId(); ?>">
		<div class="card">
			<img class="avatar" src="dynamic/avatars/<?= $contributor->getAvatarLink(); ?>" alt="Contributor's profile picture" /><br>
			<strong><?= $contributor->getName(); ?></strong>
			<?php if (!empty($contributor->getLore())) : ?>
			<br>
			<i><?= $contributor->getLore() ?></i>
			<?php endif ?>
			<ul>
			<?php foreach ($contributor->getRoles() as $role) : ?>
				<li style="color:#<?= $role->color ?>;"><?= $role->name ?></li>
			<?php endforeach ?>
			</ul>
		</div>
	</a></div>
<?php endforeach ?>

