DB_ROOT_PASSWORD=rootpassword
DB_USER=vowuser
DB_PASSWORD=password

# Storage Configuration
# =====================
# STORAGE_TYPE options:
#   - "local" = Use local filesystem (default, stored in ./dynamic/ directory)
#   - "azure" = Use Azure Blob Storage (requires connection string below)
#
# For local development: Use STORAGE_TYPE=local
# For production: Use STORAGE_TYPE=azure
#
# To switch to Azure Blob Storage:
# 1. Create an Azure Storage Account (https://portal.azure.com)
# 2. Create a container named "vow" with "Blob" public access level
# 3. Get your connection string from: Storage Account > Access Keys > Connection String
# 4. Set AZURE_STORAGE_CONNECTION_STRING below (format: DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net)
# 5. Set STORAGE_TYPE=azure
# 6. Comment out the dynamic_data volume mount in docker-compose.yml
# 7. Restart containers: docker-compose -f docker-compose.dev.yml restart web
#
STORAGE_TYPE=local
AZURE_STORAGE_CONNECTION_STRING=

#Line reporting keys
LINE_REPORT_COLLECT=testing
LINE_REPORT_MODIFY=testing

#Usage analysis api keys
STATISTICS_AGGREGATE=testing

#Discord integration key
DISCORD_INTEGRATION=testing

#Premium authenticator key
PREMIUM_AUTHENTICATION=testing